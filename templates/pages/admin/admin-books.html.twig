{% extends 'Layouts/admin.html.twig' %}

{% block content %}
    <div class="col-md-10 offset-md-1 col-lg-9 offset-lg-0">
        <!-- Recently Favorited -->
        <div class="widget dashboard-container my-adslist">
            <div class="row">
                <div class="col-md-10">
                    <h2 class="widget-header">Les livres</h2>
                </div>
                <div class="col-md-2 action">
                    <a data-toggle="modal" data-placement="top" title="Add" class="add" href="" data-target="#additem">
                        <i class="fa fa-plus-circle"></i>
                    </a>
                </div>
            </div>
            <table class="table table-responsive product-dashboard-table">
                <thead>
                <tr>
                    <th class="text-center">Image</th>
                    <th class="text-center">Manga</th>
                    <th class="text-center">Tome</th>
                    <th class="text-center">Auteur</th>
                    <th class="text-center">Action</th>
                </tr>
                </thead>
                <tbody>

                {% for manga in mangas %}
                    <tr>
                        <td class="product-thumb">
                            <img width="120px" height="auto" src="{{ asset('uploads/'~manga.photo.url) }}" alt="image description"></td>
                        <td class="product-details">
                            <h3 class="title">{{ manga.frenchTitle }}</h3>
                            <div class="overflow-auto p-2" style=" height: 100px"><span class="summery">{{ manga.summary }}</span></div>
                        </td>
                        <td class="tomes"><span class="categories">56</span></td>
                        <td class="author"><span class="categories">{{ manga.author }}</span></td>
                        <td class="action" data-title="Action">
                            <div class="">
                                <ul class="list-inline justify-content-center">
                                    <li class="list-inline-item">
                                        <a data-toggle="tooltip" data-placement="top" title="View" class="view" href="{{ url('single', {slug: manga.slug}) }}">
                                            <i class="fa fa-eye"></i>
                                        </a>
                                    </li>
                                    <li class="list-inline-item">
                                        <a data-toggle="tooltip" data-placement="top" title="Edit" class="edit" href="{{ url('edit_manga', {id: manga.id}) }}">
                                            <i class="fa fa-edit"></i>
                                        </a>
                                    </li>
                                    <li class="list-inline-item">
                                        <a data-placement="top" title="Delete" class="delete" href="{{ url('delete', {id: manga.id}) }}" data-toggle="modal" data-target="#deleteitem">
                                            <i class="fa fa-trash"></i>
                                        </a>
                                    </li>
                                    <li class="list-inline-item">
                                        <a data-toggle="tooltip" data-placement="top" title="Add" class="add" href="{{ url('admin-tome', {slug: manga.slug}) }}" >
                                            <i class="fa fa-book"></i>
                                        </a>
                                    </li>
                                </ul>

                            </div>
                        </td>
                    </tr>

                {% endfor %}
                </tbody>
            </table>

        </div>

        <!-- pagination -->
        <div class="pagination justify-content-center">
            <nav aria-label="Page navigation example">
                <ul class="pagination">
                    <li class="page-item">
                        <a class="page-link" href="#" aria-label="Previous">
                            <span aria-hidden="true">&laquo;</span>
                            <span class="sr-only">Previous</span>
                        </a>
                    </li>
                    <li class="page-item"><a class="page-link" href="#">1</a></li>
                    <li class="page-item active"><a class="page-link" href="#">2</a></li>
                    <li class="page-item"><a class="page-link" href="#">3</a></li>
                    <li class="page-item">
                        <a class="page-link" href="#" aria-label="Next">
                            <span aria-hidden="true">&raquo;</span>
                            <span class="sr-only">Next</span>
                        </a>
                    </li>
                </ul>
            </nav>
        </div>
        <!-- pagination -->

    </div>
{% endblock content %}

